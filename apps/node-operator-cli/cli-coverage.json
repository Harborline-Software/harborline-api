{
  "schemaVersion": 1,
  "note": "Ticket 072 (harborline-control): the operator CLI's coverage manifest. Every method+route pair (whole graph - wave-1 finding opcli-1 removed the audience filter as an evasion channel) in the node's sealed executable endpoint graph must appear here, either under 'verbs' (the CLI command that drives it - its command-contract test pins the exact route) or under 'exemptions' with a rationale of substance. CliCoverageReconciliationTests reconciles this file against the discovered graph in both directions: an uncovered operator route fails by name, and a stale entry naming a route the graph no longer serves also fails.",
  "verbs": {
    "* /ready": "health",
    "GET /api/local-node/teams": "tenant list",
    "POST /api/local-node/packs/install": "pack install",
    "POST /api/local-node/packs/activate": "pack activate",
    "POST /api/local-node/packs/deactivate": "pack deactivate",
    "POST /api/local-node/data-exports": "export",
    "POST /api/local-node/entities": "entity create",
    "POST /api/local-node/packs/export": "pack export",
    "POST /api/local-node/packs/verify": "pack verify"
  },
  "exemptions": {
    "* /api/llm/{**rest}": "Session-gated LLM reverse-proxy transport; plumbing for the web client, not an operator action.",
    "* /health": "Pre-auth browser/liveness probes; the CLI's health verb uses /ready, the canonical readiness probe.",
    "* /live": "Pre-auth browser/liveness probes; the CLI's health verb uses /ready, the canonical readiness probe.",
    "* /ws": "Peer/device WebSocket transport plane, not an operator HTTP action.",
    "DELETE /api/local-node/contacts/{id}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "DELETE /api/local-node/contacts/{id}/roles/{roleId}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "DELETE /api/local-node/forms/{formId}/drafts/{caseId}": "Interactive Forms product surface for the desktop client; authoring and case work are product workflows, not node operation.",
    "DELETE /api/local-node/invoices/{id}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/accounting-periods": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/accounting/outstanding": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/accounting/summary": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/admission/identity": "Founder web-admission ceremony: each step binds an interactive browser session, so a headless verb cannot complete the ceremony alone.",
    "GET /api/local-node/approval-tasks": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/approval-tasks/{instanceId}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/asset-registry/entities": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/asset-registry/entities/{id}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/asset-registry/entities/{id}/condition": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/asset-registry/entities/{id}/submissions": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/asset-registry/entities/{id}/tree": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/asset-registry/types": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/asset-registry/types/{id}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/authorization/capability-definitions": "The surface's consumer is the app's Authorization editor (ticket 220 slice 3); the CLI gains verbs only if an operator workflow needs them.",
    "GET /api/local-node/authorization/capability-definitions/{definitionId:guid}/binding": "The surface's consumer is the app's Authorization editor (ticket 220 slice 3); the CLI gains verbs only if an operator workflow needs them.",
    "GET /api/local-node/authorization/holders": "Access holders inspection for the app's Access workspace (tickets 329 and 301): projects parties, grants and attribution failures under members:manage. This slice serves the interactive administration surface; a headless holdings-audit verb is deferred until an operator workflow requires it.",
    "GET /api/local-node/authorization/role-vocabulary": "The surface's consumer is the app's Authorization editor (ticket 220 slice 3); the CLI gains verbs only if an operator workflow needs them.",
    "GET /api/local-node/authorization/traces/{auditId:guid}": "Ticket 331: interactive decision disclosure for the app authorization editors, addressed by an audit id from save or refusal feedback. A headless trace-inspection verb is deferred until an operator workflow requires it.",
    "GET /api/local-node/authorization/standing-catalogue": "The surface's consumer is the app's Authorization editor (ticket 220 slice 3); the CLI gains verbs only if an operator workflow needs them.",
    "GET /api/local-node/audit-events": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/audit-events/{auditId}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/bank-accounts": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/bank-accounts/{accountId}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/bank-accounts/{accountId}/match-proposals": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/bank-accounts/{accountId}/reconciliation-state": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/bank-accounts/{accountId}/statement-lines": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/bills": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/bills/{id}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/bills/{id}/payments": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/calendar/calendars": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/calendar/free-busy": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/calendar/occurrences": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/channels": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/chart-of-accounts": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/chart-of-accounts/{id}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/charts": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/comms": "Interactive Comms product surface for the desktop client; conversation and DM traffic is product usage, not node operation.",
    "GET /api/local-node/comms/dm/roster": "Interactive Comms product surface for the desktop client; conversation and DM traffic is product usage, not node operation.",
    "GET /api/local-node/comms/dm/{otherPartyId}": "Interactive Comms product surface for the desktop client; conversation and DM traffic is product usage, not node operation.",
    "GET /api/local-node/comms/{conversationId}": "Interactive Comms product surface for the desktop client; conversation and DM traffic is product usage, not node operation.",
    "GET /api/local-node/consent-records/{recordId}": "Interactive subject-consent product workflow for the desktop/web client; reading, requesting, activating, expiring, and revoking tenant consent records are product governance actions, not node operation.",
    "GET /api/local-node/contacts": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/contacts/{id}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/current-principal-signature": "Desktop session introspection for the bound principal; meaningful only inside an interactive desktop session.",
    "GET /api/local-node/data-exchange/definitions": "Interactive Data Exchange admin surface for the desktop client (ticket 087); pack-projected definition reads are product configuration workflows, not node operation.",
    "GET /api/local-node/data-exchange/definitions/{key}": "Interactive Data Exchange admin surface for the desktop client (ticket 087); pack-projected definition reads are product configuration workflows, not node operation.",
    "GET /api/local-node/data-exchange/definitions/{key}/versions": "Interactive Data Exchange admin surface for the desktop client (ticket 087); pack-projected definition reads are product configuration workflows, not node operation.",
    "GET /api/local-node/data-exchange/definitions/{key}/versions/{version}": "Interactive Data Exchange admin surface for the desktop client (ticket 087); pack-projected definition reads are product configuration workflows, not node operation.",
    "GET /api/local-node/data-exports/{exportId:guid}": "OPERATOR-GAP: export status/download by id - natural companions to the export verb (the headless E2E polls these via service today). Tracked as ticket 018 remaining CLI scope.",
    "GET /api/local-node/data-exports/{exportId:guid}/download": "OPERATOR-GAP: export status/download by id - natural companions to the export verb (the headless E2E polls these via service today). Tracked as ticket 018 remaining CLI scope.",
    "GET /api/local-node/documents": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/documents/{id}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/documents/{id}/content": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/entities": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/forms/definitions": "Interactive Forms product surface for the desktop client; authoring and case work are product workflows, not node operation.",
    "GET /api/local-node/forms/definitions/{formId}": "Interactive Forms product surface for the desktop client; authoring and case work are product workflows, not node operation.",
    "GET /api/local-node/forms/definitions/{formId}/versions": "Interactive Forms product surface for the desktop client; authoring and case work are product workflows, not node operation.",
    "GET /api/local-node/forms/definitions/{formId}/versions/{version}": "Interactive Forms product surface for the desktop client; authoring and case work are product workflows, not node operation.",
    "GET /api/local-node/forms/drafts": "Interactive Forms product surface for the desktop client; authoring and case work are product workflows, not node operation.",
    "GET /api/local-node/forms/{formId}": "Interactive Forms product surface for the desktop client; authoring and case work are product workflows, not node operation.",
    "GET /api/local-node/forms/{formId}/drafts/{caseId}": "Interactive Forms product surface for the desktop client; authoring and case work are product workflows, not node operation.",
    "GET /api/local-node/governance/lifecycle": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/invoices": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/invoices/{id}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/invoices/{id}/payments": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/journal-entries": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/journal-entries/{id}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/kg-action-tasks": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/kg-action-tasks/{instanceId}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/kg/search": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/leases": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/leases/{name}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/leases/{name}/payment-history": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/maintenance": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/maintenance/{name}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/navigation/workspaces": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/org-branding": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/org-branding/logo": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/packs/compose/{id}": "Pack composition authoring surface for the desktop composer UI; composing a pack is content authoring, not node operation.",
    "GET /api/local-node/packs/graph": "OPERATOR-GAP: pack-lifecycle read (list/graph/preview) - candidate verbs pairing with pack install/activate. Tracked as ticket 018 remaining CLI scope.",
    "GET /api/local-node/packs/installed": "OPERATOR-GAP: pack-lifecycle read (list/graph/preview) - candidate verbs pairing with pack install/activate. Tracked as ticket 018 remaining CLI scope.",
    "GET /api/local-node/payments": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/payments/{id}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/payroll/employees": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/payroll/pay-runs": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/payroll/pay-runs/{id}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/properties": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/properties/{name}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/recurring-invoices": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/recurring-invoices/{id}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/reports/definitions": "Interactive Reports admin surface for the desktop client (ticket 085); pack-projected definition reads are product configuration workflows, not node operation.",
    "GET /api/local-node/reports/definitions/{key}": "Interactive Reports admin surface for the desktop client (ticket 085); pack-projected definition reads are product configuration workflows, not node operation.",
    "GET /api/local-node/reports/definitions/{key}/versions": "Interactive Reports admin surface for the desktop client (ticket 085); pack-projected definition reads are product configuration workflows, not node operation.",
    "GET /api/local-node/reports/definitions/{key}/versions/{version}": "Interactive Reports admin surface for the desktop client (ticket 085); pack-projected definition reads are product configuration workflows, not node operation.",
    "GET /api/local-node/scheduling/definitions": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/scheduling/definitions/{definitionId}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/scheduling/definitions/{definitionId}/versions": "Interactive Scheduling product surface for the desktop/web client (ticket 088); revision history and append-only restore are product configuration workflows, not node operation.",
    "GET /api/local-node/scheduling/definitions/{definitionId}/versions/{revision:int}": "Interactive Scheduling product surface for the desktop/web client (ticket 088); revision history and append-only restore are product configuration workflows, not node operation.",
    "GET /api/local-node/scheduling/subjects": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/spatial-frames/{anchor}/{frameCode}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/spatial-frames/{anchor}/{frameCode}/{frameEpoch}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/status": "OPERATOR-GAP: node/sync status are genuine candidates for status verbs. Tracked as ticket 018 remaining CLI scope; exempted so the debt is visible here rather than silent.",
    "GET /api/local-node/sync-status": "OPERATOR-GAP: node/sync status are genuine candidates for status verbs. Tracked as ticket 018 remaining CLI scope; exempted so the debt is visible here rather than silent.",
    "GET /api/local-node/tables": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/teams/active": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/views/definitions": "Interactive Views admin surface for the desktop client (ticket 086); pack-projected definition reads are product configuration workflows, not node operation.",
    "GET /api/local-node/views/definitions/{key}": "Interactive Views admin surface for the desktop client (ticket 086); pack-projected definition reads are product configuration workflows, not node operation.",
    "GET /api/local-node/views/definitions/{key}/versions": "Interactive Views admin surface for the desktop client (ticket 086); pack-projected definition reads are product configuration workflows, not node operation.",
    "GET /api/local-node/views/definitions/{key}/versions/{version}": "Interactive Views admin surface for the desktop client (ticket 086); pack-projected definition reads are product configuration workflows, not node operation.",
    "GET /api/local-node/workflow-confirmations": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/workflow-confirmations/{instanceId}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/workflow-run-report": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/workflows/definitions": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/local-node/workflows/definitions/{key}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "GET /api/session/admin/invitations": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "GET /api/session/admin/members": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "GET /api/session/antiforgery": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "GET /api/session/me": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "GET /api/session/permissions": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "GET /api/session/whoami": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "PATCH /api/local-node/maintenance/{name}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/accounting-periods/open": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/accounting-periods/{id}/close": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/admission/invites": "Founder web-admission ceremony: each step binds an interactive browser session, so a headless verb cannot complete the ceremony alone.",
    "POST /api/local-node/admission/join": "Founder web-admission ceremony: each step binds an interactive browser session, so a headless verb cannot complete the ceremony alone.",
    "POST /api/local-node/admission/redeem": "Founder web-admission ceremony: each step binds an interactive browser session, so a headless verb cannot complete the ceremony alone.",
    "POST /api/local-node/approval-tasks/{instanceId}/action": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/asset-registry/edges": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/asset-registry/entities": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/asset-registry/types": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/asset-registry/types/{id}/revert": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bank-accounts": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bank-accounts/{accountId}/accept-match": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bank-accounts/{accountId}/archive": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bank-accounts/{accountId}/feed/connect": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bank-accounts/{accountId}/feed/disconnect": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bank-accounts/{accountId}/feed/pull": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bank-accounts/{accountId}/import-statement": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bank-accounts/{accountId}/opening-balance": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bank-accounts/{accountId}/reconciliation-state/lock": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bank-accounts/{accountId}/reconciliation-state/unlock": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bank-accounts/{accountId}/un-match": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bills": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bills/{id}/approve": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bills/{id}/dispute": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bills/{id}/payments": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bills/{id}/resolve-dispute": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/bills/{id}/void": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/calendar/calendars": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/channels/{id}/check": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/chart-of-accounts": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/chart-of-accounts/seed-from-template": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/chart-of-accounts/{id}/archive": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/comms": "Interactive Comms product surface for the desktop client; conversation and DM traffic is product usage, not node operation.",
    "POST /api/local-node/comms/dm/{otherPartyId}": "Interactive Comms product surface for the desktop client; conversation and DM traffic is product usage, not node operation.",
    "POST /api/local-node/comms/{conversationId}": "Interactive Comms product surface for the desktop client; conversation and DM traffic is product usage, not node operation.",
    "POST /api/local-node/compromised-devices/{partyId}/respond": "OPERATOR-GAP: the ticket 056 compromised-device response path is a security-operator action; a headless verb needs its own ADR 0018-aware ruling first. Tracked as ticket 018 remaining CLI scope.",
    "POST /api/local-node/consent-records": "Interactive subject-consent product workflow for the desktop/web client; reading, requesting, activating, expiring, and revoking tenant consent records are product governance actions, not node operation.",
    "POST /api/local-node/consent-records/{recordId}/activate": "Interactive subject-consent product workflow for the desktop/web client; reading, requesting, activating, expiring, and revoking tenant consent records are product governance actions, not node operation.",
    "POST /api/local-node/consent-records/{recordId}/expire": "Interactive subject-consent product workflow for the desktop/web client; reading, requesting, activating, expiring, and revoking tenant consent records are product governance actions, not node operation.",
    "POST /api/local-node/consent-records/{recordId}/revoke": "Interactive subject-consent product workflow for the desktop/web client; reading, requesting, activating, expiring, and revoking tenant consent records are product governance actions, not node operation.",
    "POST /api/local-node/contacts": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/contacts/{id}/delete": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/contacts/{id}/roles": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/contacts/{id}/update": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/document-templates/issue": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/document-templates/render": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/documents": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/documents/{id}/attach": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/forms/definitions/{formId}/restore": "Interactive Forms product surface for the desktop client; authoring and case work are product workflows, not node operation.",
    "POST /api/local-node/forms/{formId}/submit": "Interactive Forms product surface for the desktop client; authoring and case work are product workflows, not node operation.",
    "POST /api/local-node/governance/lifecycle/finish-setup": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/import/erpnext/preview": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/invoices": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/invoices/{id}/issue": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/invoices/{id}/payments": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/invoices/{id}/void": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/invoices/{id}/write-off": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/journal-entries": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/journal-entries/{id}/reverse": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/kg-action-tasks/{instanceId}/action": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/leases": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/leases/{name}/activate-subledger": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/maintenance": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/org-branding/logo": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/packs/compose": "Pack composition authoring surface for the desktop composer UI; composing a pack is content authoring, not node operation.",
    "POST /api/local-node/packs/compose/{id}/affirm": "Pack composition authoring surface for the desktop composer UI; composing a pack is content authoring, not node operation.",
    "POST /api/local-node/packs/compose/{id}/export": "Pack composition authoring surface for the desktop composer UI; composing a pack is content authoring, not node operation.",
    "POST /api/local-node/packs/preview": "OPERATOR-GAP: pack-lifecycle read (list/graph/preview) - candidate verbs pairing with pack install/activate. Tracked as ticket 018 remaining CLI scope.",
    "POST /api/local-node/payroll/employees": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/payroll/pay-runs": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/payroll/pay-runs/{id}/post": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/payroll/pay-runs/{id}/reverse": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/properties": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/recurring-invoices": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/recurring-invoices/{id}/archive": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/recurring-invoices/{id}/generate": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/recurring-invoices/{id}/pause": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/recurring-invoices/{id}/resume": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/authorization/capability-definitions/{definitionId:guid}/binding": "The surface's consumer is the app's Authorization editor (ticket 220 slice 3); the CLI gains verbs only if an operator workflow needs them.",
    "POST /api/local-node/reports/ap-aging-summary": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/reports/ar-aging-summary": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/reports/balance-sheet": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/reports/profit-and-loss": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/reports/profit-and-loss-by-property": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/reports/trial-balance": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/scheduling/appointments": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/scheduling/definitions/validate": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/scheduling/definitions/{definitionId}/restore": "Interactive Scheduling product surface for the desktop/web client (ticket 088); revision history and append-only restore are product configuration workflows, not node operation.",
    "POST /api/local-node/scheduling/events": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/scheduling/resources/availability": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/local-node/workflow-confirmations/{instanceId}/action": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "POST /api/session/account-challenge": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "POST /api/session/account-setup-accept": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "POST /api/session/admin/invitations": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "POST /api/session/admin/members/permissions": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "POST /api/session/admin/members/narrow": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "POST /api/session/admin/members/revoke": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "POST /api/session/connect-device": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "POST /api/session/founder-bind": "Desktop founder-bind ceremony; binds the interactive desktop session itself and has no headless meaning.",
    "POST /api/session/login": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "POST /api/session/logout": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "POST /api/session/recovery-accept": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "POST /api/session/select": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "POST /api/session/switch": "Interactive web-session or admission ceremony; binds browser session state (cookies, antiforgery, device connect), so a headless verb cannot complete it alone.",
    "POST /membrane/invoke": "Internal membrane command plane; reachable only through the capability chokepoint contract, never a CLI surface.",
    "PUT /api/local-node/asset-registry/types/{id}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "PUT /api/local-node/forms/definitions/{formId}": "Interactive Forms product surface for the desktop client; authoring and case work are product workflows, not node operation.",
    "PUT /api/local-node/forms/{formId}/drafts/{caseId}": "Interactive Forms product surface for the desktop client; authoring and case work are product workflows, not node operation.",
    "PUT /api/local-node/org-branding": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "PUT /api/local-node/scheduling/definitions/{definitionId}/draft": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class.",
    "PUT /api/local-node/workflows/definitions/{key}": "Interactive product surface for the desktop/web client; product data and workflows are not node operation. Whole-graph rule per wave-1 finding opcli-1: every pair is named here so a new operator route can never hide in an audience class."
  }
}
