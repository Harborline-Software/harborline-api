{
  "$schema": "https://json.schemastore.org/stryker-config",
  "stryker-config": {
    "project": "Harborline.LocalNodeHost.csproj",
    "since": { "enabled": false, "target": "origin/main", "ignore-changes-in": ["**/stryker-config.json"] },
    "thresholds": { "high": 80, "low": 60, "break": 0 },
    "reporters": ["json", "progress"]
  }
}
