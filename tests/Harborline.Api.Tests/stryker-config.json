{
  "$schema": "https://json.schemastore.org/stryker-config",
  "stryker-config": {
    "project": "Harborline.Api.Contracts.csproj",
    "since": { "enabled": true, "target": "origin/main", "ignore-changes-in": ["**/stryker-config.json"] },
    "thresholds": { "high": 80, "low": 60, "break": 60 },
    "reporters": ["json", "progress"]
  }
}
