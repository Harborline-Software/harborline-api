# eng/legacy-env-prefix-allowlist.tsv — ticket 245.
# <path>\t<class>\t<reason>. Exact file rows, no wildcards. The list must equal the set the scan
# discovers: a row whose file no longer matches fails the scan (see eng/legacy-env-prefix-scan.sh).
apps/capability-host/src/runtime/operational-environment.ts	refusal	The TypeScript runtime guard: it must name the retired prefix to refuse a stale name loudly.
apps/capability-host/scripts/operational-environment.mjs	refusal	The script-side copy of the same guard, loaded by the standalone runner mains.
apps/capability-host/src/runtime/operational_environment.py	refusal	The Python worker guard: the four standalone workers inherit the environment and refuse the same way.
apps/local-node-host/Data/Search/CapabilityHostOperationalEnvironment.cs	refusal	The host-side guard applied before every capability spawn.
apps/capability-host/src/runtime/operational-environment.test.ts	refusal-test	Proves the TypeScript guard refuses a stale name and passes a CAPABILITY_HOST_ one.
apps/local-node-host/tests/Health/CapabilityHostOperationalEnvironmentCompositionTests.cs	refusal-test	Real-composition proof that the registered providers refuse a stale name at spawn.
eng/baselines/hull-test-baseline.json	pinned-baseline	A pinned baseline (eng/baselines is not edited by slices); its prose records the pre-rename gate name.
