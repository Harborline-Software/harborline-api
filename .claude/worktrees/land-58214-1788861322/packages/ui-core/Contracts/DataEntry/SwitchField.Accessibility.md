# SwitchField — Accessibility Contract (Redirect)

- **Component:** SwitchField
- **Status:** Deprecated — consolidated into Switch
- **Contract type:** Accessibility

> **This component has been consolidated into Switch.**
> SwitchField is now a thin deprecated shim that delegates to Switch.
> See [Switch.Accessibility.md](./Switch.Accessibility.md) for the unified contract.
>
> Migrate: replace `<SwitchField>` with `<Switch>` inside `<FormField>`.
> Switch now reads FormFieldContext automatically.
>
> SwitchField will be removed in the next major release.
