# ToggleSwitch — Styling Contract (Redirect)

- **Component:** ToggleSwitch
- **Status:** Deprecated — consolidated into Switch
- **Contract type:** Styling

> **This component has been consolidated into Switch.**
> See [Switch.Styling.md](./Switch.Styling.md) for the unified contract.
>
> Migrate: replace `<ToggleSwitch>` with `<Switch>`.
> Note: ToggleSwitch used hard-coded `bg-blue-600`/`bg-gray-200`; Switch uses semantic
> design tokens (`bg-primary`/`bg-muted`) that respect the active theme.
