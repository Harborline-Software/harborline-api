# TextField — Accessibility Contract (Redirect)

- **Component:** TextField
- **Status:** Deprecated — consolidated into TextBox
- **Contract type:** Accessibility

> **This component has been consolidated into TextBox.**
> TextField is now a thin deprecated shim that delegates to TextBox.
> See [TextBox.Accessibility.md](./TextBox.Accessibility.md) for the unified contract.
>
> Migrate: replace `<TextField>` with `<TextBox>` inside `<FormField>`.
> TextBox now reads FormFieldContext automatically.
>
> TextField will be removed in the next major release.
