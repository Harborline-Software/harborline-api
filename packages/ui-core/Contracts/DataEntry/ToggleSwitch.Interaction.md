# ToggleSwitch — Interaction Contract (Redirect)

- **Component:** ToggleSwitch
- **Status:** Deprecated — consolidated into Switch
- **Contract type:** Interaction

> **This component has been consolidated into Switch.**
> See [Switch.Interaction.md](./Switch.Interaction.md) for the unified contract.
>
> Migrate: replace `<ToggleSwitch>` with `<Switch>`.
> `onChange` maps to `onCheckedChange`; `description` is now a native Switch prop.
