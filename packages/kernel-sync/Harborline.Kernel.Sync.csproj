<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <PackageId>Harborline.Api.Kernel.Sync</PackageId>
    <AssemblyName>Harborline.Api.Kernel.Sync</AssemblyName>
    <RootNamespace>Harborline.Api.Kernel.Sync</RootNamespace>
    <Description>Intra-team gossip anti-entropy daemon for the Harborline local-node architecture (paper §6.1, §6.2; ADR 0029). Implements the sync-daemon wire protocol — HELLO / CAPABILITY_NEG / ACK / DELTA_STREAM / LEASE_* / GOSSIP_PING / ERROR — over a CBOR-framed Unix-socket or named-pipe transport, with a leaderless random-peer-selection gossip scheduler on a configurable tick. Sibling to but distinct from federation-* (cross-organizational) per ADR 0029.</Description>
    <PackageTags>harborline;kernel;sync;gossip;p2p;local-first</PackageTags>
    <!-- Stub interfaces and XML-doc gaps will be filled during review hardening.
         CS1591 kept noisy-but-off until the first stable release, matching the
         Foundation and Kernel facade packages. -->
    <NoWarn>$(NoWarn);CS1591</NoWarn>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
    <PackageReference Include="Microsoft.Extensions.Options" />
    <PackageReference Include="System.Formats.Cbor" />
    <PackageReference Include="Makaretu.Dns.Multicast.New" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\foundation\Harborline.Foundation.csproj" />
    <ProjectReference Include="..\ui-core\Harborline.UICore.csproj" />
    <ProjectReference Include="..\kernel\Harborline.Kernel.csproj" />
    <ProjectReference Include="..\kernel-event-bus\Harborline.Kernel.EventBus.csproj" />
    <ProjectReference Include="..\kernel-crdt\Harborline.Kernel.Crdt.csproj" />
    <ProjectReference Include="..\kernel-security\Harborline.Kernel.Security.csproj" />
  </ItemGroup>
  <ItemGroup>
    <!-- Exclude the sibling test project from library compilation. -->
    <Compile Remove="tests/**/*.cs" />
  </ItemGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="Harborline.Api.Kernel.Sync.Tests" />
  </ItemGroup>
</Project>
